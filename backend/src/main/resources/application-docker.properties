spring.application.name=backend
server.port=8080

# Database connection (?????????? .env ???? docker-compose)
spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:Apartment_management}
spring.datasource.username=${DB_USERNAME:Devops}
spring.datasource.password=${DB_PASSWORD:Devops555}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
